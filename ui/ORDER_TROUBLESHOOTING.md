# 주문하기 화면 문제 해결 가이드

## 🔍 문제 진단

주문하기 화면이 정상적으로 작동하지 않는 경우, 다음을 확인하세요.

---

## ✅ 확인 사항

### 1. 브라우저 개발자 도구 확인

**F12** 키를 눌러 개발자 도구를 열고 다음을 확인:

#### Console 탭
- 에러 메시지 확인
- `주문 시작:`, `주문 데이터 준비 시작:`, `주문 API 호출...` 등의 로그 확인
- 빨간색 에러 메시지 확인

#### Network 탭
- `/api/orders` POST 요청 확인
- 요청 상태 코드 확인:
  - ✅ 201: 주문 생성 성공
  - ❌ 400: 잘못된 요청 데이터
  - ❌ 404: API 엔드포인트를 찾을 수 없음
  - ❌ 500: 서버 내부 오류

---

## 🐛 일반적인 문제와 해결 방법

### 문제 1: 메뉴가 표시되지 않음

**증상:**
- 주문하기 화면에 메뉴가 보이지 않음
- "메뉴가 없습니다" 메시지 표시

**해결:**
1. 브라우저 콘솔에서 에러 확인
2. "다시 시도" 또는 "메뉴 다시 불러오기" 버튼 클릭
3. API 서버 연결 확인

---

### 문제 2: 장바구니에 추가되지 않음

**증상:**
- 메뉴를 클릭해도 장바구니에 추가되지 않음
- "품절" 메시지가 계속 표시됨

**해결:**
1. 재고 확인:
   - 관리자 화면에서 재고 수량 확인
   - 재고가 0이면 재고 수정 필요
2. 브라우저 콘솔에서 에러 확인
3. 페이지 새로고침 (F5)

---

### 문제 3: 주문하기 버튼이 작동하지 않음

**증상:**
- 주문하기 버튼을 클릭해도 아무 반응이 없음
- 주문이 생성되지 않음

**해결:**
1. **브라우저 콘솔 확인:**
   - F12 → Console 탭
   - `주문 시작:`, `주문 API 호출...` 로그 확인
   - 에러 메시지 확인

2. **Network 탭 확인:**
   - F12 → Network 탭
   - 주문하기 버튼 클릭
   - `/api/orders` POST 요청 확인
   - 요청 상태 코드 확인

3. **장바구니 확인:**
   - 장바구니에 아이템이 있는지 확인
   - 장바구니가 비어있으면 "장바구니가 비어있습니다" 메시지 표시

---

### 문제 4: 주문 생성 실패

**증상:**
- 주문하기 버튼 클릭 후 에러 메시지 표시
- "주문 생성에 실패했습니다" 메시지

**가능한 원인:**

#### 원인 1: 재고 부족
**해결:**
- 관리자 화면에서 재고 확인
- 재고가 부족한 메뉴의 재고 수정

#### 원인 2: API 서버 오류
**해결:**
1. Render 대시보드에서 백엔드 서비스 상태 확인
2. Logs 탭에서 에러 확인
3. 데이터베이스 연결 확인

#### 원인 3: 잘못된 요청 데이터
**해결:**
- 브라우저 콘솔에서 `주문 데이터:` 로그 확인
- 데이터 형식이 올바른지 확인

---

## 🔧 디버깅 단계

### 1단계: 브라우저 콘솔 확인

1. **F12** 키를 눌러 개발자 도구 열기
2. **Console** 탭 클릭
3. 주문하기 버튼 클릭
4. 다음 로그 확인:
   ```
   주문 시작: [...]
   주문 데이터 준비 시작: [...]
   주문 데이터: {...}
   주문 API 호출...
   주문 API 응답: {...}
   주문 결과: {...}
   ```

### 2단계: Network 탭 확인

1. **Network** 탭 클릭
2. 주문하기 버튼 클릭
3. `/api/orders` 요청 찾기
4. 요청 상세 정보 확인:
   - **Status**: 201 (성공), 400/404/500 (실패)
   - **Request Payload**: 전송된 데이터 확인
   - **Response**: 서버 응답 확인

### 3단계: API 직접 테스트

브라우저 주소창에서 직접 테스트:

```
https://your-api.onrender.com/api/orders
```

POST 요청은 브라우저에서 직접 테스트할 수 없으므로, Postman이나 curl을 사용하거나 Network 탭에서 확인하세요.

---

## 📝 체크리스트

주문하기 화면 문제 해결:

- [ ] 브라우저 개발자 도구(F12) → Console 탭에서 에러 확인
- [ ] Network 탭에서 `/api/orders` POST 요청 확인
- [ ] 요청 상태 코드 확인 (201, 400, 404, 500 등)
- [ ] 장바구니에 아이템이 있는지 확인
- [ ] 재고가 충분한지 확인 (관리자 화면)
- [ ] API 서버가 실행 중인지 확인 (Render 대시보드)
- [ ] 환경 변수 `VITE_API_BASE_URL` 설정 확인
- [ ] 페이지 새로고침 (Ctrl + F5 또는 Cmd + Shift + R)

---

## 💡 추가 팁

1. **캐시 문제:**
   - 브라우저 캐시를 지우고 다시 시도
   - 시크릿 모드에서 테스트

2. **네트워크 문제:**
   - 인터넷 연결 확인
   - 다른 네트워크에서 테스트

3. **서버 상태:**
   - Render Free Plan은 15분 비활성 시 sleep
   - 첫 요청 시 약 30초 소요 가능

---

## 🆘 여전히 문제가 있는 경우

1. **브라우저 콘솔의 전체 에러 메시지 복사**
2. **Network 탭의 실패한 요청 상세 정보 확인**
3. **Render Logs의 에러 메시지 확인**
4. 위 정보를 바탕으로 문제 진단

