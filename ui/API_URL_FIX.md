# API URL 문제 해결 가이드

## 🔍 문제 발견

환경 변수 `VITE_API_BASE_URL`에 `/api`가 누락되어 있습니다.

**현재 설정:**
```
VITE_API_BASE_URL=https://order-app-bankend.onrender.com
```

**올바른 설정:**
```
VITE_API_BASE_URL=https://order-app-bankend.onrender.com/api
```

---

## ✅ 해결 방법

### 방법 1: 환경 변수 수정 (권장)

1. **Render 대시보드** 접속
2. **프론트엔드 Static Site** 선택
3. **"Environment"** 탭 클릭
4. `VITE_API_BASE_URL` 환경 변수 수정:

   **현재 (잘못된 값):**
   ```
   https://order-app-bankend.onrender.com
   ```

   **수정 (올바른 값):**
   ```
   https://order-app-bankend.onrender.com/api
   ```

5. **"Save Changes"** 클릭
6. 자동 재배포 대기 (약 2-3분)

### 방법 2: 코드 자동 수정 (이미 적용됨)

코드에서 자동으로 `/api`를 추가하도록 수정했습니다. 환경 변수가 `/api` 없이 설정되어 있어도 자동으로 추가됩니다.

**주의:** 환경 변수를 올바르게 설정하는 것이 더 명확하고 안정적입니다.

---

## 🔧 확인 방법

### 배포 후 확인:

1. 브라우저 개발자 도구(F12) → Console 탭
2. 다음 로그 확인:
   ```
   API_BASE_URL: https://order-app-bankend.onrender.com/api
   VITE_API_BASE_URL env: https://order-app-bankend.onrender.com/api
   ```

3. API 요청 확인:
   ```
   API 요청: https://order-app-bankend.onrender.com/api/menus/admin
   ```

4. 응답 상태 확인:
   - ✅ 200: 성공
   - ❌ 404: 여전히 문제 있음 (백엔드 라우트 확인 필요)

---

## 📝 환경 변수 설정 체크리스트

- [ ] `VITE_API_BASE_URL` 값이 `https://order-app-bankend.onrender.com/api` 형식인지 확인
- [ ] URL 끝에 `/api`가 포함되어 있는지 확인
- [ ] URL 끝에 슬래시(`/`)가 없는지 확인 (예: `/api/` ❌)
- [ ] 환경 변수 저장 후 재배포 완료 대기
- [ ] 브라우저 새로고침 (Ctrl + F5)

---

## ⚠️ 주의사항

1. **URL 형식:**
   - ✅ 올바름: `https://order-app-bankend.onrender.com/api`
   - ❌ 잘못됨: `https://order-app-bankend.onrender.com` (끝에 `/api` 없음)
   - ❌ 잘못됨: `https://order-app-bankend.onrender.com/api/` (끝에 슬래시)

2. **오타 확인:**
   - `bankend` → `backend` (오타가 있지만, 실제 백엔드 URL과 일치해야 함)
   - 실제 백엔드 URL을 확인하여 정확히 입력

3. **재배포:**
   - 환경 변수 변경 후 자동으로 재배포됨 (약 2-3분)
   - 재배포 완료 후 브라우저 새로고침 필요

---

## 🆘 여전히 404 오류가 발생하는 경우

1. **백엔드 서버 확인:**
   - Render 대시보드에서 백엔드 서비스 상태 확인
   - 백엔드 URL 직접 접속 테스트:
     ```
     https://order-app-bankend.onrender.com/
     ```
     → `{"message":"커피 주문 앱 API 서버","version":"1.0.0"}` 응답이 와야 함

2. **API 엔드포인트 확인:**
   ```
   https://order-app-bankend.onrender.com/api/menus
   ```
   → 메뉴 목록 JSON 응답이 와야 함

3. **백엔드 라우트 확인:**
   - 백엔드 `server/src/app.js`에서 라우트 설정 확인
   - `/api/menus`, `/api/orders` 라우트가 올바르게 설정되어 있는지 확인

